name: Hugo Deploy

# Publishes end-user docs to GitHub Pages on release tags only (DEC-021).
# Pull requests validate Hugo builds via hugo-ci.yml without publishing.

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    uses: ./.github/workflows/hugo-build.yml
    with:
      deploy: true
