// Must stay in sync with ThemePreferenceConstants.cs (storage key and palette backgrounds).
window.soloDevBoardThemePreference = Object.freeze({
    storageKey: 'solo-dev-board.theme-preference',
    lightBackground: '#ffffff',
    darkBackground: '#0d1117',
});
